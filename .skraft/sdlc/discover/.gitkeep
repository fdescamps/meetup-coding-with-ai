# This .gitkeep file ensures that the .skraft/sdlc/discover/ directory structure
# is tracked by git, even when empty. This allows workflows to persistently
# write triage reports and sprint proposals to this location.
