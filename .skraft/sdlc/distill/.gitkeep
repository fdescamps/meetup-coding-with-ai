# This .gitkeep file ensures that the .skraft/sdlc/distill/ directory structure
# is tracked by git, even when empty. This allows workflows to persistently
# write BDD scenarios and test plans to this location.
